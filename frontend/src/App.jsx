/**
 * App.jsx — Entry point for the authenticated workspace.
 * Renders the new 3-column SaaS DashboardLayout.
 */
import DashboardLayout from './components/DashboardLayout';

export default function App() {
  return <DashboardLayout />;
}
